<%= form_with(model: @teacher, local: true) do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>

  <%= form.label :bio %>
  <%= form.text_area :bio %>

  <%= form.submit 'Update Teacher' %>
<% end %>
